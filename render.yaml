services:
  - type: web
    name: presaige-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PRESAIGE_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false